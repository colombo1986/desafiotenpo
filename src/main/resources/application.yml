server:
  port: 8080

randomnumberapi:
  base-url: http://www.randomnumberapi.com/api/v1.0

springdoc:
  show-actuator: true
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

spring:
  application:
    name: cvergara
  r2dbc:
    url: r2dbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:mydb}
    username: ${DB_USER:myuser}
    password: ${DB_PASSWORD:mypassword}

  data:
    r2dbc:
      repositories:
        enabled: true
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

  flyway:
    enabled: true
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/mydb}
    user: ${SPRING_DATASOURCE_USERNAME:myuser}
    password:  ${SPRING_DATASOURCE_PASSWORD:mypassword}
    locations: classpath:db/migration
    baseline-on-migrate: true

  sql:
   init:
    mode: never # Deshabilita la inicialización automática de scripts SQL para evitar conflictos con Flyway




resilience4j:
  ratelimiter:
    instances:
      historyRateLimiter:
        limit-for-period: 3          # Número máximo de solicitudes
        limit-refresh-period: 60s     # Período de refresco (60 segundos)
        timeout-duration: 0s          # Tiempo de espera para arrojar fallo



logging:
  level:
    io.r2dbc: INFO
    io.r2dbc.postgresql: INFO
    org.springframework.data.r2dbc.core: INFO


